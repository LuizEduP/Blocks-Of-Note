{
  "name": "blocks-of-note",
  "version": "1.0.0",
  "private": true,
  "description": "Blocks-Of-Note — Gerenciador de notas e tarefas com interface 3D",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vitest": "^3.1.0"
  }
}
